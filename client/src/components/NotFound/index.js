import React from "react";

const NotFound = () => (
  <div>
    <h1>
      Sorry we are Unable to connet your requested path <br />
      it seams the path may not exit
    </h1>
  </div>
);

export default NotFound;
